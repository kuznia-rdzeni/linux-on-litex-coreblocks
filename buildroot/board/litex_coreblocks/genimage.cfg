image boot.vfat {
    vfat {
        files = {
            "boot.json",
            "Image",
            "rootfs.cpio.gz",
            "rv32.dtb"
        }
    }
    size = 16M
}

image sdcard.img {
    hdimage {
    }

    partition boot {
        partition-type = 0xC
        bootable = "true"
        image = "boot.vfat"
    }
}
